# SHACL benchmarks (`benchmarks/shacl/`)

| Directory | Gradle project | Role |
|-----------|----------------|------|
| [`jmh/`](jmh/) | `:benchmarks:shacl` | JMH harness, workloads, baselines |
| [`era-cli/`](era-cli/) | `:benchmarks:shacl-era-cli` | ERA-SHACL-Benchmark CLI / Docker sample |

Gradle task names stay **`:benchmarks:shacl`** and **`:benchmarks:shacl-era-cli`**; build outputs remain **`build/modules/benchmarks/shacl/`** and **`build/modules/benchmarks/shacl-era-cli/`**.
