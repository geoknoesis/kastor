# Tier B — synthetic stress

Reserved for **generated** graphs (deterministic seeds, scale parameters). The current JMH micro-benchmark uses `src/jmh/resources/jmh-workload/` directly.

Future: add JSON descriptors that reference generator specs or checked-in scale-N Turtle under `workloads/tier-b/generated/`.
